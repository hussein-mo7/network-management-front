# Proxied to backend via vite (see vite.config.ts) — must include /v1
VITE_API_BASE_URL=/api/v1

# true = dev login without API; false = real auth against backend (admin / admin123)
VITE_SKIP_AUTH_CHECK=false
